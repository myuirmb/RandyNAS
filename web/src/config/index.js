const config = {
    service: {
        url: 'http://localhost:13579',
        interface: {
            init:'/init',
            login: '/login',
            menu:'/menu'
        }
    }
};

export default config;